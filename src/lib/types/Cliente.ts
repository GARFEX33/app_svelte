export interface Cliente {
    id: number;
    cliente: string;
    razonsocial: string;
    // Otros campos relevantes según tu base de datos
  }
  
  export type ClienteSupabase = {
    id: number;
    cliente: string;
    razon_social: string;
};

